# Workspace format v1

A Noura workspace is a normal directory with `workspace.yaml` at its root. Markdown files with valid managed frontmatter become workspace objects. Other files remain ordinary files.

## Manifest

```yaml
id: workspace_01k...
format_version: 1
name: Example workspace
created: 2026-08-27T18:00:00Z
updated: 2026-08-27T18:00:00Z
enabled_plugins:
  - folders
  - notes
  - tasks
  - calendar
  - projects
ignore: []
```

The workspace ID identifies the workspace across moves. Device-local indexes and credentials live in operating system application-data and credential directories.

`enabled_plugins` lists the identifiers of the modules the workspace turns on. Rules:

- Plugin identifiers are a lowercase letter followed by lowercase letters, digits, or hyphens, at most 64 characters total.
- The list is deduplicated and order is not significant.
- An empty list is valid and enables no domain modules.
- Unknown identifiers are preserved and tolerated so future ecosystem plugins do not break older builds; readers simply ignore ids they do not implement.
- Application plugin toggles rewrite this file atomically and refresh `updated`.

## Managed Markdown

```markdown
---
id: task_01k...
type: task
status: todo
priority: medium
project: null
due: 2026-09-04
created: 2026-08-27T18:00:00Z
updated: 2026-08-27T18:00:00Z
---

# Prepare release notes

Summarize user-visible changes.
```

IDs use a lowercase object-type prefix and lowercase ULID. Timestamps use RFC 3339 UTC. Date properties accept `YYYY-MM-DD` for all-day values or RFC 3339 with an explicit offset for timed values.

The serializer writes known fields in canonical order and sorts unknown properties by key. It preserves unknown JSON-compatible values. It does not preserve YAML comments, aliases, tags, or the original formatting after an application mutation.

## Classification

- A file with valid `id` and `type` fields becomes a managed object.
- Idless Markdown remains unmanaged until explicit adoption.
- Malformed frontmatter remains untouched and searchable as raw content.
- Duplicate IDs remain untouched. ID-addressed operations stop with `identity_conflict` until the user resolves the files.

## Identity and revisions

Paths describe current locations. A move or rename does not change the frontmatter ID. The content revision is the BLAKE3 hash of the current file bytes. Update, move, and delete operations require the revision observed by the caller.

## Local metadata

`.noura/trash/<timestamp>/<original-relative-path>` stores recoverable application deletes. `.noura/history/<object-id>/` stores Markdown snapshots that let users recover versions displaced during external-edit reconciliation. Scanners ignore `.noura`. SQLite indexes, process locks, provider configuration, recent workspaces, and credentials do not belong in the workspace.
