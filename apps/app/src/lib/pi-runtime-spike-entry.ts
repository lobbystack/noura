// This entry is built separately in CI to prove Pi Agent Core bundles for the
// WebView without importing its Node-only entry point. It is not application UI.
export { createPiRuntimeSpike } from '@noura/ai';
